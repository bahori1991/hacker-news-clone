{
  "name": "@apps/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "tsdown",
    "compile": "bun build --compile --minify --soucemap --bytecode ./src/index.ts --outfile server",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@packages/auth": "workspace:*",
    "@packages/db": "workspace:*",
    "@packages/env": "workspace:*",
    "@packages/shared": "workspace:*",
    "hono": "^4.11.3",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@packages/tsconfig": "workspace:*",
    "@types/bun": "^1.3.5",
    "tsdown": "^0.18.3"
  }
}
